/**
 *  OPERADORES LOGICOS
*/

//AND, OR Y NOT - Y, O , NEGACIÓN(NO)

// && : OPERADOR Y
// || : OPERADOR O
// ! : OPERADOR DE NEGACIÓN

const a = 10
const b = 20
const c = "10"

(a === c && a === c) // false
(a != b || a === c) //true
!(a === c) //true